{
  "name": "papyrus",
  "private": true,
  "scripts": {
    "dev": "pnpm run --parallel dev",
    "build": "pnpm run --filter @papyrus/shared build && pnpm run --parallel build",
    "lint": "pnpm run --recursive lint",
    "test": "pnpm run --recursive test"
  },
  "devDependencies": {
    "typescript": "^5.4.0"
  }
}
